{
  "name": "shopease-mern-ecommerce",
  "version": "1.0.0",
  "private": true,
  "description": "Full stack MERN e-commerce application (React, Express, Node.js, MongoDB)",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "server": "npm run dev --prefix backend",
    "client": "npm run dev --prefix frontend",
    "dev": "concurrently -n BACKEND,FRONTEND -c green,cyan \"npm run server\" \"npm run client\"",
    "seed": "npm run seed --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
